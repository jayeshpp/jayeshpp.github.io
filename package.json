{
  "name": "jayeshpp.github.io",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://jayeshpp.github.io/build",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d build",
    "deploy_profile": "gh-pages -b profile -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}